{
  "name": "yaml-query",
  "version": "1.0.0",
  "description": "A Github Action for populating environment variables with values queried from yaml files.",
  "main": "action.js",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "js-yaml": "^4.1.0"
  }
}
